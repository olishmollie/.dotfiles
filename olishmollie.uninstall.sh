#!/usr/bin/env bash

# Remove dotfiles, vim and git config

rm ~/.bash_profile
rm ~/.spacemacs
rm ~/.vimrc
rm ~/.irbrc
rm ~/.inputrc
rm ~/.gitignore_global
rm ~/.gitconfig
rm ~/.bashrc

rm -rf ~/.vim
rm -rf ~/.profile

rm ~/olishmollie.uninstall.sh
